{
  "name": "clean-up-css",
  "version": "1.0.0",
  "description": "clean up the css that is no longer used on the project",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node ./index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "@types/yargs": "^17.0.33",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "yargs": "^18.0.0"
  }
}
